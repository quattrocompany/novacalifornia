# Copie para .env.local para rodar localmente. Em produção, configure estas
# variáveis em: Vercel > Project Settings > Environment Variables.

# --- Supabase (leads) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Firebase (Kit Corretor - leitura pública dos materiais) ---
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# --- NOVO: Firebase Admin SDK (usado só no servidor, para upload/exclusão
# autenticados do Kit Corretor) ---
# Gere em: Firebase Console > Configurações do Projeto > Contas de Serviço >
# Gerar nova chave privada. Cole o JSON inteiro em uma linha (ou em base64).
FIREBASE_SERVICE_ACCOUNT_KEY=

# --- NOVO: Login do painel administrativo ---
# JSON com usuário:senha. Troque as senhas antigas (que estavam no código) —
# considere-as comprometidas (eram as mesmas em todos os sites da Quattro).
# Exemplo: {"vendrix":"defina-uma-senha-forte","marketing":"defina-outra-senha-forte"}
ADMIN_USERS=

# NOVO: string longa e aleatória usada para assinar o cookie de sessão do
# admin (ex.: gere com `openssl rand -hex 32`). Use um valor DIFERENTE do
# usado nos outros sites (Lumini 3 / Ocean Park).
ADMIN_SESSION_SECRET=

# --- E-mail (Resend) ---
RESEND_API_KEY=
# NOVO (opcional): destinatário(s) do e-mail de notificação de novo lead.
# Aceita vários e-mails separados por vírgula. Se não definido, usa o
# endereço padrão que já estava no código.
LEADS_NOTIFICATION_EMAIL=estandenovacalifornia@gmail.com

# --- reCAPTCHA ---
RECAPTCHA_SECRET_KEY=
